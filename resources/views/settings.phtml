<?php
/** @var string $title */
/** @var string $config_url */
/** @var bool $enabled */
/** @var bool $allow_guests */
/** @var bool $allow_attachments */
/** @var bool $notify_managers */
/** @var bool $acknowledge_contributors */
/** @var bool $show_fact_links */
/** @var bool $show_family_links */
/** @var bool $show_media_links */
/** @var int $max_upload_mb */
/** @var bool $table_ready */
/** @var bool $attachments_ready */
/** @var string $attachment_root */
/** @var string $version */
?>

<div class="container-xl py-3">
    <div class="d-flex justify-content-between align-items-start gap-3 flex-wrap mb-3">
        <div>
            <h2 class="mb-1"><?= e($title) ?></h2>
            <p class="text-muted mb-0">Collect research contributions without giving visitors direct GEDCOM edit access.</p>
        </div>
        <span class="badge bg-secondary">Version <?= e($version) ?></span>
    </div>

    <div class="alert alert-light border">
        Contributions are stored separately from the GEDCOM. Nothing is added to the family tree or media library automatically.
    </div>

    <?php if (!$table_ready || !$attachments_ready) : ?>
        <div class="alert alert-danger">
            One or more Family Contributions database tables are unavailable. Check database permissions before accepting submissions.
        </div>
    <?php endif ?>

    <form method="post" action="<?= e($config_url) ?>">
        <?php if (function_exists('csrf_field')) : ?>
            <?= csrf_field() ?>
        <?php endif ?>

        <div class="card mb-3">
            <div class="card-header"><strong>Availability</strong></div>
            <div class="card-body">
                <div class="form-check mb-3">
                    <input class="form-check-input" type="checkbox" id="pfc-enabled" name="enabled" value="1" <?= $enabled ? 'checked' : '' ?>>
                    <label class="form-check-label" for="pfc-enabled">Enable Potts Family Contributions</label>
                </div>

                <div class="form-check mb-0">
                    <input class="form-check-input" type="checkbox" id="pfc-guests" name="allow_guests" value="1" <?= $allow_guests ? 'checked' : '' ?>>
                    <label class="form-check-label" for="pfc-guests">Allow visitors who are not signed in to contribute</label>
                    <div class="form-text">Signed-in members can contribute whenever the module is enabled.</div>
                </div>
            </div>
        </div>

        <div class="card mb-3">
            <div class="card-header"><strong>Where contribution links appear</strong></div>
            <div class="card-body">
                <div class="form-check mb-3">
                    <input class="form-check-input" type="checkbox" id="pfc-fact-links" name="show_fact_links" value="1" <?= $show_fact_links ? 'checked' : '' ?>>
                    <label class="form-check-label" for="pfc-fact-links">Facts and events</label>
                    <div class="form-text">Shows a subtle <strong>Suggest correction</strong> action on eligible individual and spouse-family facts. The submission keeps a snapshot of the fact as it appeared at the time.</div>
                </div>

                <div class="form-check mb-3">
                    <input class="form-check-input" type="checkbox" id="pfc-family-links" name="show_family_links" value="1" <?= $show_family_links ? 'checked' : '' ?>>
                    <label class="form-check-label" for="pfc-family-links">Family pages and the individual Families tab</label>
                    <div class="form-text">Lets visitors contribute general family information or corrections to family facts such as marriage and divorce.</div>
                </div>

                <div class="form-check mb-0">
                    <input class="form-check-input" type="checkbox" id="pfc-media-links" name="show_media_links" value="1" <?= $show_media_links ? 'checked' : '' ?>>
                    <label class="form-check-label" for="pfc-media-links">Media pages and the individual Media tab</label>
                    <div class="form-text">Lets visitors identify people, places, dates, events or objects shown in a media item and provide supporting information.</div>
                </div>
            </div>
        </div>

        <div class="card mb-3">
            <div class="card-header"><strong>Attachments</strong></div>
            <div class="card-body">
                <div class="form-check mb-3">
                    <input class="form-check-input" type="checkbox" id="pfc-attachments" name="allow_attachments" value="1" <?= $allow_attachments ? 'checked' : '' ?>>
                    <label class="form-check-label" for="pfc-attachments">Allow one PDF or image attachment with each contribution</label>
                </div>

                <div class="mb-3" style="max-width:220px">
                    <label class="form-label" for="pfc-max-upload">Maximum attachment size (MB)</label>
                    <input class="form-control" id="pfc-max-upload" name="max_upload_mb" type="number" min="1" max="25" value="<?= e((string) $max_upload_mb) ?>">
                </div>

                <div class="alert alert-light border small mb-0">
                    Allowed files: PDF, JPG, PNG and WEBP. Attachments are stored privately outside the module folder and are never added to webtrees media automatically.<br>
                    <span class="text-muted">Storage folder:</span> <code><?= e($attachment_root) ?></code>
                </div>
            </div>
        </div>

        <div class="card mb-3">
            <div class="card-header"><strong>Email notifications</strong></div>
            <div class="card-body">
                <div class="form-check mb-3">
                    <input class="form-check-input" type="checkbox" id="pfc-notify" name="notify_managers" value="1" <?= $notify_managers ? 'checked' : '' ?>>
                    <label class="form-check-label" for="pfc-notify">Email the tree contact and support users when a contribution is received</label>
                </div>

                <div class="form-check mb-2">
                    <input class="form-check-input" type="checkbox" id="pfc-ack" name="acknowledge_contributors" value="1" <?= $acknowledge_contributors ? 'checked' : '' ?>>
                    <label class="form-check-label" for="pfc-ack">Email the contributor an acknowledgement</label>
                </div>

                <div class="form-text">Email uses the SMTP or sendmail configuration already set in webtrees. Email failure never prevents a contribution from being stored.</div>
            </div>
        </div>

        <button class="btn btn-primary" type="submit">Save settings</button>
    </form>
</div>
